public class StoreRunner {
  public static void main(String[] args) {

    // Instantiate a FileStore object
    FileStore fs = new FileStore();

    // Starts the user interaction for the store
    fs.startStore();
    
  }
}